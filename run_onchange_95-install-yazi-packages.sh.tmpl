#!/bin/bash
# {{ include "dot_config/yazi/package.toml" | sha256sum }}

set -euo pipefail

if ! command -v mise &>/dev/null; then
  echo "mise is not installed yet; skipping yazi package install."
  exit 0
fi

mise exec -C "$HOME" -- ya pkg install
